<h1>AllGameAnalysis#view</h1>
  <div class="row">
<% @data[:headers].each do |header|%>
    <div class="column header"><%= header %></div>
<% end %>
  </div>
<% @data[:data].each do |row|%>
  <div class="row <%= if (row[-1].to_f < 0.05) then "highlight" end %>">
    <% row.each do |col| %>
    <div class="column"><%= col %></div>
    <% end %>
  </div>
<% end %>
